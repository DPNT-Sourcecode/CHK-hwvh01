

# noinspection PyUnusedLocal
# skus = unicode string
def checkout(skus):

    aCount = int(0)
    bCount = int(0)

    if isinstance(skus, str):
        for sku in skus:
            if != sku.isalnum():
                if sku = 'a':
                    aCount += 1
                elif sku = 'b'


    else:
        return -1

